# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
*.egg-info/
dist/
build/
*.egg
.eggs/

# Virtual environments
.venv/
venv/
env/
ENV/

# macOS
.DS_Store
.AppleDouble
.LSOverride
._*

# IDE / editor
.vscode/
.idea/
*.swp
*.swo

# Jupyter
.ipynb_checkpoints/
*.ipynb

# Logs / tmp
*.log
.dash_*_restart.log
*.tmp
*.temp
/tmp/

# Dash / Flask
flask_session/
.cache/

# Environment files (keep .env.example, not secrets)
.env
.env.local

# Editor backups
*.bak

# Generated poster artifacts (regenerated by the make_*_poster.py scripts)
*.pptx

# Claude Code memory (private session files)
.claude/

# dashboard/data is the CANONICAL clean dataset (Real Madrid matches, all
# competitions/seasons, 0 corrupt) — it is tracked and deployed. The raw root
# folders below are the unverified provider dump (some files corrupt) and are
# intentionally ignored. Leading slash = repo-root only, so dashboard/data/... is kept.
/LaLiga/
/Copa del Rey/
/UEFA Champions League/

# Duplicate cloned folder (contains only a README, has its own .git)
-FINAL_MASTER_PROJECT-REALMADRID-/
