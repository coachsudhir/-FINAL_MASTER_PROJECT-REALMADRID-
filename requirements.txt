# Runtime dependencies for the Real Madrid CF Tactical Dashboard (Dash app).
# Slimmed to what the app actually imports — faster, more reliable Render builds.
dash>=2.18.0
dash-bootstrap-components>=1.5.0
plotly==5.18.0
pandas>=2.2.0
numpy>=1.26.4
scipy>=1.13.0          # scipy.spatial.ConvexHull — position-map team shape
gunicorn==21.2.0       # production WSGI server (Render start command)
